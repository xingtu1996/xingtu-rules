# XingTu Rules · 专业自述

> 定位：行途开源矩阵的规则工程化资产仓，把团队规范 / 编码约定 / 流程要求变成 AI 可执行的 rules，并提供分层规则库。

## 1. 这个项目是干什么的（作用）

- **给谁用**：想用 AI 协作规则约束 Agent 行为的技术管理者、一线工程师；想让"团队规范"不再靠人盯、而是被 Agent 自动遵守的团队。
- **解决什么问题**：口头规范传不进 Agent 上下文、放不进 prompt 的规范等于没有。rules 把规范写成**机器可加载**的文件，Agent 启动即读取，行为被前置约束，而不是事后返工。
- **核心价值**：
  1. **可执行**：规则文件放到约定目录即生效，Agent 会遵守，不靠人脑记忆。
  2. **分层设计**：全局规则（`rules/`，所有任务生效）、按场景/目录生效（`rules-scoped/`）、按需触发（`rules-on-demand/`）三层，粒度从"永远"到"特定时刻"。
  3. **AI 可检索**：每个规则文件统一 frontmatter（name + description + 适用场景），工具能按语义检索到正确的规则。
  4. **可沉淀**：一次踩坑 → 一条规则 → 全体 Agent 受益，规则库是团队经验的固化层。
  5. **与 harness 打通**：作为子模块被 xingtu-harness 聚合，随场景安装分发。

## 2. 在行途 harness 中的定位

- **位置**：护栏层（L2）。harness 六层架构里，rules 是"红线与边界"的载体——宪法/合规要求/创作铁律写成全局规则常驻，场景性约束走 scoped，临时性约束走 on-demand。
- **协作关系**：

```
             ┌────────────────────────────────────────────┐
             │  Agent 会话（所有 Work / Coding Agent）       │
             └──────────────────┬─────────────────────────┘
                                │ 启动即加载（常驻约束）
        ┌───────────────────────┴───────────────────────┐
        │  xingtu-rules（本仓）                            │
        │  rules/          → 全局，所有任务生效             │
        │  rules-scoped/   → 目录/场景级，按上下文生效       │
        │  rules-on-demand/→ 按需触发，用户或 Agent 显式加载 │
        └───────────────────────┬───────────────────────┘
                                │
        ┌───────────────────────┴───────────────────────┐
        │  xingtu-skills（L3 方法技能） xingtu-sdd（任务规格）│
        └───────────────────────────────────────────────┘
```

- **与相邻仓的关系**：rules 管"**不能做什么 / 必须怎么风格**"的常驻约束；skills 管"**用什么方法做**"的按需能力；specs 管"**单个任务做到什么标准**"的临时规格。三者叠加：规则框边界，技能给方法，规格定验收。

## 3. 与其他项目的差异与区别

| 对比项 | xingtu-rules | xingtu-skills | xingtu-ai-engineering（governance/宪法） | xingtu-sdd |
|--------|-------------|---------------|------------------------------------------|------------|
| 内容形态 | 机器可加载的规则文件（frontmatter + 条款） | SKILL.md 文档式技能 | 方法论论述 + 宪法模板 | 任务级规格流程（00-06 编号） |
| 加载方式 | 放对目录即自动加载（确定性） | LLM 检索 description 后按需调用 | 人读后用，需再转写成 rules | 单任务显式创建，随任务生命周期 |
| 生效范围 | 常驻，作用于所有/特定上下文 | 触发时才生效 | 理念层，指导规则怎么写 | 任务级，用完即止 |
| 边界 | 约束行为、禁止项、风格 | 提供方法、流程、判断依据 | 讲清"为什么这么设计" | 讲清"这个任务做到什么算完" |
| 何时选它 | 想让 Agent 一直遵守某条约束 | 想让 Agent 会做某类事 | 想设计整套治理体系 | 想让单个复杂任务可控可复现 |

一句话：**宪法是"为什么"，rules 是"落地成什么"，skills 是"怎么做"，specs 是"单个任务做到什么算完"**。

## 4. 在 Agent 体系中的应用

### 4.1 Work Agent（业务/内容工作流）

- **作用方法**：合规红线、内容口径、创作铁律写成全局规则，所有内容类 Agent 会话启动即读取，产出在进入分发前就被约束住，而不是发布后追责。
- **触发方法**：全局规则常驻；场景规则在 Agent 进入对应目录/任务时生效；临时要求用 on-demand 规则显式加载。
- **典型场景**：自媒体内容合规检查、对外口径统一、风格（去 AI 味）约束、脱敏要求。

### 4.2 Coding Agent（编码 Agent，如 Claude Code）

- **作用方法**：把编码约定、禁止事项、分支策略、提交规范写进规则文件，Agent 在改动代码时自动遵守。
- **触发方法**：放入 `.claude/rules/` 自动加载；scoped 规则通过子目录标记按目录生效；on-demand 规则由用户显式要求时加载。
- **典型场景**：禁止动某些文件、必须保留接口兼容、提交信息格式、测试门禁、安全底线（不做破坏性操作）。

## 5. 升级方法与迭代开发

- **新增规则**：从 `rules/_TEMPLATE` 复制，写清 frontmatter（name / description / 适用场景）与条款本身。
- **质量门禁**：
  - 每条规则必须有明确的触发与不触发边界，避免两条规则打架或互相覆盖。
  - 规则要被真实任务验证过：放进去之后观察 Agent 是否遵守、是否误伤正常行为；误伤就收窄，失守就补强。
  - 禁止规则写"空话条款"（如"要严谨"），必须可被 Agent 具体执行。
- **演进路径**：先补全三层目录（scoped / on-demand 实例），再沉淀规则方法论文档，说明"什么规范适合写成规则、怎么写才不打架"。
- **当前状态**：`rules/` 目录与 `_TEMPLATE` 已就绪，分层规则库与方法论文档待填充。

## 6. 基础概念

- **rules（规则文件）**：描述 Agent 应遵守约束的 Markdown 文件，通常带 frontmatter 便于检索。
- **scoped rule（按范围生效）**：规则只在特定目录/上下文生效，用于"这个模块必须这样写"类约束。为什么重要：避免全局规则在不适用的地方误伤。
- **on-demand rule（按需触发）**：不常驻、用户或 Agent 显式要求时才加载，适合低频或高成本的约束。
- **frontmatter**：文件头部的 YAML 元数据（name / description），是 AI 检索与匹配的依据——描述写得越准，命中越准。
- **护栏（guardrail）**：一组"绝对禁止/必须满足"的规则，通常不可被任务目标覆盖。为什么重要：AI 会"过度配合"，护栏是防过度配合的最后一道闸。
- **规则优先级与冲突消解**：全局 vs scoped、通用 vs 具体、旧 vs 新 之间的生效顺序；不设计优先级，规则就会打架。
- **宪法 / constitution**：更高层、更稳定的一小撮规则，作为其它规则的"上位法"，通常不随项目频繁变动。

## 7. 专业背书

- Claude Code 原生支持 `.claude/rules/` 目录，并支持通过子目录标记实现 scoped rules（只对特定目录生效），本仓分层设计与其机制一一对应。
- `AGENTS.md` 是业界推动的跨工具 Agent 指令开放标准，多个主流工具（含 Claude Code、Codex、Cursor 等）已支持；本仓 frontmatter + 统一结构与之对齐，保证"一份规则，多工具通用"。
- 规则工程化呼应 Anthropic 官方关于 Agent 安全与行为控制的实践（以文件化、可审计的方式约束模型行为，而非依赖 prompt 口头叮嘱），也呼应"prompt injection 防护 + 护栏优先"的工程共识。
